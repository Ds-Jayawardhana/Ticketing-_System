import React from 'react'
import { Button } from '@/components/ui/button'

interface Props {}

const Home = (props: Props) => {
  return (
    <Button variant="secondary">This Is Home</Button>
  )
}
export default Home
